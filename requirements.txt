# Web
fastapi==0.115.*
uvicorn[standard]==0.34.*
jinja2==3.1.*
python-multipart==0.0.*
itsdangerous==2.2.*
pydantic==2.*
pyyaml==6.*
httpx==0.28.*

# Model engine (CPU). Install CPU torch via the CPU wheel index (see Dockerfile
# --extra-index-url) so no multi-GB CUDA wheels are pulled.
torch==2.4.*
transformers==4.44.*
accelerate==1.*
safetensors==0.4.*
huggingface_hub==0.25.*
hf_transfer==0.1.*        # optional faster pulls; enabled via HF_HUB_ENABLE_HF_TRANSFER=1

# Secrets (prod only; harmless in dev)
azure-identity==1.*
azure-keyvault-secrets==4.*

# Tests
pytest==8.*
